--- src/components/PaymentFlow.tsx
+++ src/components/PaymentFlow.tsx
@@ -4,7 +4,7 @@
 import { TransactionSession } from '../types';
 import { rtdb, ref, set, update, db, doc, setDoc } from '../firebase';
-import { ArrowLeft, Lock, Shield, Clock, CheckCircle, AlertCircle } from 'lucide-react';
+import { ArrowLeft, Lock, Shield, Clock, CheckCircle, AlertCircle, Clipboard } from 'lucide-react';
 import { InstructionsModal } from './InstructionsModal';
 import { PhoneChatLockIcon, CheckMarkBoxIcon } from './Icons';
 
@@ -236,7 +236,13 @@
             />
-            {otpValue.length === 6 && (
+            {otpValue.length === 6 ? (
               <div className="absolute right-4 top-1/2 -translate-y-1/2 text-green-500">
                  <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M5 13l4 4L19 7" /></svg>
+              </div>
+            ) : (
+              <div className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">
+                 <Clipboard className="w-5 h-5" />
               </div>
             )}
           </div>
